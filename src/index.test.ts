import { Hideable, List, Show, ShowIfElse, Switch } from '.';

test('List component should be defined', () => {
  expect(List).toBeDefined();
});

test('Show component should be defined', () => {
  expect(Show).toBeDefined();
});

test('ShowIfElse component should be defined', () => {
  expect(ShowIfElse).toBeDefined();
});

test('Switch component should be defined', () => {
  expect(Switch).toBeDefined();
});

test('Hideable component should be defined', () => {
  expect(Hideable).toBeDefined();
});
