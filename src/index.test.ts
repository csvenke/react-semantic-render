import { Map, Resolve, Show, Switch } from './index';

test('Map', () => {
  expect(Map).toBeDefined();
});

test('Show', () => {
  expect(Show).toBeDefined();
});

test('Switch', () => {
  expect(Switch).toBeDefined();
});

test('Resolve', () => {
  expect(Resolve).toBeDefined();
});
